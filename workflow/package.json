{
  "name": "@platforma-open/milaboratories.import-sc-rnaseq-data.workflow",
  "version": "1.1.3",
  "type": "module",
  "description": "Block Workflow",
  "scripts": {
    "build": "rm -rf dist && pl-tengo check && pl-tengo build",
    "test": "vitest",
    "format": "/usr/bin/env emacs --script ./format.el"
  },
  "dependencies": {
    "@platforma-sdk/workflow-tengo": "catalog:"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:",
    "@platforma-open/milaboratories.import-sc-rnaseq-data.software": "workspace:*",
    "@platforma-open/milaboratories.gene-annotations-assets": "catalog:",
    "@platforma-sdk/test": "catalog:",
    "vitest": "catalog:"
  }
}